## Handoff: team-plan → team-exec
- **Decided**: Execute `.omc/plans/20260416_122818_litellm_injection_viz.md` via 3 parallel workers + 1 consolidation task + 1 gated rerun. Split: worker-1 = Phase 1 transport, worker-2 = Phase 2 injection taxonomy, worker-3 = Phase 4a viz module. worker-1 then picks up consolidation Task #4 (Phase 3 harness rewrite + Phase 4b viz hook + legacy delete) once tasks 1/2/3 land. Task #5 (live rerun) is GATED — team-lead handles after user confirms $8-15 spend.
- **Rejected**: (a) Single worker sequential — too slow. (b) 5 parallel workers — three of them would collide on `system_tests/test_single_file_stress.py`. (c) litellm Python SDK — proxy is OpenAI-shaped so env-swap suffices (ADR in plan).
- **Risks**: worker-2 edits `evaluate_fixes` branches (lines 448-531) in `test_single_file_stress.py` while worker-1 stays out of that file. worker-3 touches only NEW files + pyproject.toml. Consolidation (Task #4) handles MODELS dict + viz hook + legacy delete all on `test_single_file_stress.py` — no concurrency on that file after Phase 2. gpt-oss model id `gpt-oss-120b` unconfirmed — worker-3 / consolidation verifies via `$OPENAI_BASE_URL/models`.
- **Files**: `.omc/plans/20260416_122818_litellm_injection_viz.md` (plan w/ 11 Analyst findings folded in, 10 ACs, changelog at bottom).
- **Remaining**: Phase 1 (6 steps), Phase 2 (5 steps), Phase 4a (4 steps), consolidation Task #4 (8 steps), Phase 5 rerun (8-step runbook, gated).
